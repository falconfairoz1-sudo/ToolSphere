import PowerPointCompressor from '@/components/tools/PowerPointCompressor';

export const metadata = {
  title: 'PowerPoint Compressor | Reduce File Size',
  description: 'Compress PowerPoint files to reduce size without losing quality',
};

export default function PowerPointCompressorPage() {
  return <PowerPointCompressor />;
}
