import PDFProtect from '@/components/tools/PDFProtect';

export const metadata = {
  title: 'Protect PDF - Add Password Protection',
  description: 'Add password protection to PDF documents. Free, fast, and secure PDF protection tool.',
};

export default function PDFProtectPage() {
  return <PDFProtect />;
}
