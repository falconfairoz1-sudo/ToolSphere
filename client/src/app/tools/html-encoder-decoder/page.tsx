import HTMLEncoderDecoder from '@/components/tools/HTMLEncoderDecoder';

export const metadata = {
  title: 'HTML Encoder/Decoder - Encode & Decode HTML Entities',
  description: 'Safely encode and decode HTML special characters and entities',
};

export default function HTMLEncoderDecoderPage() {
  return <HTMLEncoderDecoder />;
}
