import SalaryBreakdown from '@/components/tools/SalaryBreakdown';

export const metadata = {
  title: 'Salary Breakdown Calculator - Finance Tools | ToolHub',
  description: 'Calculate CTC to in-hand salary with detailed breakdown',
};

export default function SalaryBreakdownPage() {
  return <SalaryBreakdown />;
}
