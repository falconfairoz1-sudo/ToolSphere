import WorldClock from '@/components/tools/WorldClock';

export const metadata = {
  title: 'World Clock - View Time Across Timezones',
  description: 'Check current time in major cities around the world',
};

export default function WorldClockPage() {
  return <WorldClock />;
}
