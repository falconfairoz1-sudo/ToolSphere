import ClipboardManager from '@/components/tools/ClipboardManager';

export const metadata = {
  title: 'Clipboard Manager - ToolSphere',
  description: 'Manage your clipboard history efficiently',
};

export default function ClipboardManagerPage() {
  return <ClipboardManager />;
}
