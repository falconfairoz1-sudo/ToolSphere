import TextReplacer from '@/components/tools/TextReplacer';

export const metadata = {
  title: 'Text Find & Replace - Bulk Text Replacement',
  description: 'Find and replace text with case-sensitive options',
};

export default function TextReplacerPage() {
  return <TextReplacer />;
}
