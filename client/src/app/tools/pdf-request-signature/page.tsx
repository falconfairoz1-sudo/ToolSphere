import PDFRequestSignature from '@/components/tools/PDFRequestSignature';

export const metadata = {
  title: 'Request PDF Signature - Free Signature Request Tool',
  description: 'Request signatures from others on PDF documents. Free, fast, and easy signature request tool.',
};

export default function PDFRequestSignaturePage() {
  return <PDFRequestSignature />;
}
