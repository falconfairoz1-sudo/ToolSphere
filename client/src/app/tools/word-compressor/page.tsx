import WordCompressor from '@/components/tools/WordCompressor';

export const metadata = {
  title: 'Word Compressor | Free Online Tool',
  description: 'Reduce file size by compressing images',
};

export default function WordCompressorPage() {
  return <WordCompressor />;
}