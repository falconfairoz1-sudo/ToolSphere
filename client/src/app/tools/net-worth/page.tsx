import NetWorth from '@/components/tools/NetWorth';

export const metadata = {
  title: 'Net Worth Calculator - Finance Tools | ToolHub',
  description: 'Calculate your total net worth with assets and liabilities',
};

export default function NetWorthPage() {
  return <NetWorth />;
}
