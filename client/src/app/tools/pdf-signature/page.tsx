import PDFSignature from '@/components/tools/PDFSignature';

export const metadata = {
  title: 'Add Signature to PDF - Free Digital Signature Tool',
  description: 'Add digital signatures to PDF documents. Free, fast, and secure PDF signature tool.',
};

export default function PDFSignaturePage() {
  return <PDFSignature />;
}
