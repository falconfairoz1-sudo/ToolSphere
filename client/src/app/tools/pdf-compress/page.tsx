import PDFCompress from '@/components/tools/PDFCompress';

export const metadata = {
  title: 'PDF Compress - Free Online Tool',
  description: 'Compress PDF files with quality options (Low, Medium, High). Reduce PDF file size while maintaining quality.',
};

export default function PDFCompressPage() {
  return <PDFCompress />;
}
