import PDFMerge from '@/components/tools/PDFMerge';

export const metadata = {
  title: 'PDF Merge - ToolSphere',
  description: 'Combine multiple PDF files into one document for free',
};

export default function PDFMergePage() {
  return <PDFMerge />;
}
