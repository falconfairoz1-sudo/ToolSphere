import PowerPointMerger from '@/components/tools/PowerPointMerger';

export const metadata = {
  title: 'PowerPoint Merger | Combine Multiple Presentations',
  description: 'Merge multiple PowerPoint presentations into one file',
};

export default function PowerPointMergerPage() {
  return <PowerPointMerger />;
}
