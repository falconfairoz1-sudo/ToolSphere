import NumberFormatter from '@/components/tools/NumberFormatter';

export const metadata = {
  title: 'Number Formatter - Format Numbers with Commas',
  description: 'Format numbers with commas, spaces, scientific notation and more',
};

export default function NumberFormatterPage() {
  return <NumberFormatter />;
}
