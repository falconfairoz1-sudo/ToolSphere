import URLShortener from '@/components/tools/URLShortener';

export const metadata = {
  title: 'URL Shortener | Free Online Tool',
  description: 'Create short, memorable links with custom aliases and analytics',
};

export default function URLShortenerPage() {
  return <URLShortener />;
}
