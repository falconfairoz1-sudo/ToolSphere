import PowerPointPasswordRemover from '@/components/tools/PowerPointPasswordRemover';

export const metadata = {
  title: 'Remove PowerPoint Password | Unlock Presentations',
  description: 'Remove password protection from PowerPoint files',
};

export default function PowerPointPasswordRemoverPage() {
  return <PowerPointPasswordRemover />;
}
