import PowerPointSplitter from '@/components/tools/PowerPointSplitter';

export const metadata = {
  title: 'PowerPoint Splitter | Split Presentations',
  description: 'Split PowerPoint presentations into separate files by slides',
};

export default function PowerPointSplitterPage() {
  return <PowerPointSplitter />;
}
