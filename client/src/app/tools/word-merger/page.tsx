import WordMerger from '@/components/tools/WordMerger';

export const metadata = {
  title: 'Word Document Merger | Free Online Tool',
  description: 'Merge multiple Word documents into one with formatting preservation',
};

export default function WordMergerPage() {
  return <WordMerger />;
}
