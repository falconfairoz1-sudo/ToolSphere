import TodoList from '@/components/tools/TodoList';

export const metadata = {
  title: 'To-Do List - ToolSphere',
  description: 'Simple and efficient task management tool',
};

export default function TodoListPage() {
  return <TodoList />;
}
