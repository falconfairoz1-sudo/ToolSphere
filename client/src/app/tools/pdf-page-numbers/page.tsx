import PDFPageNumbers from '@/components/tools/PDFPageNumbers';

export const metadata = {
  title: 'PDF Page Numbering - Add Page Numbers to PDF',
  description: 'Add page numbers to PDF documents. Free, fast, and customizable PDF page numbering tool.',
};

export default function PDFPageNumbersPage() {
  return <PDFPageNumbers />;
}
