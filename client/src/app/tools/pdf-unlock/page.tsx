import PDFUnlock from '@/components/tools/PDFUnlock';

export const metadata = {
  title: 'Unlock PDF - Remove Password Protection',
  description: 'Remove password protection from PDF documents. Free, fast, and secure PDF unlock tool.',
};

export default function PDFUnlockPage() {
  return <PDFUnlock />;
}
