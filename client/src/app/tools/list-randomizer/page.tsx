import ListRandomizer from '@/components/tools/ListRandomizer';

export const metadata = {
  title: 'List Randomizer - Shuffle Lists',
  description: 'Randomly shuffle and reorder list items',
};

export default function ListRandomizerPage() {
  return <ListRandomizer />;
}
