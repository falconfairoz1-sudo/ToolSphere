import SearchablePDF from '@/components/tools/SearchablePDF';

export const metadata = {
  title: 'Make PDF Searchable - Add Text Layer with OCR',
  description: 'Convert PDF to searchable text with OCR. Make scanned PDFs searchable and accessible.',
};

export default function SearchablePDFPage() {
  return <SearchablePDF />;
}
