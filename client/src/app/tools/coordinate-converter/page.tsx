import CoordinateConverter from '@/components/tools/CoordinateConverter';
export const metadata = { title: 'Coordinate Converter | Decimal Degrees to DMS', description: 'Convert between coordinate formats' };
export default function CoordinateConverterPage() { return <CoordinateConverter />; }
