import PDFRemoveWatermark from '@/components/tools/PDFRemoveWatermark';

export const metadata = {
  title: 'Remove Watermark from PDF - Free Tool',
  description: 'Remove watermarks from PDF documents. Free, fast, and secure watermark removal tool.',
};

export default function PDFRemoveWatermarkPage() {
  return <PDFRemoveWatermark />;
}
