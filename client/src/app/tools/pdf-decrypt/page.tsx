import PDFDecrypt from '@/components/tools/PDFDecrypt';

export const metadata = {
  title: 'Decrypt PDF - Remove PDF Encryption',
  description: 'Decrypt encrypted PDF documents. Free, fast, and secure PDF decryption tool.',
};

export default function PDFDecryptPage() {
  return <PDFDecrypt />;
}
