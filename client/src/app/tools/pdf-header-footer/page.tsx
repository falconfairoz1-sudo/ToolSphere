import PDFHeaderFooter from '@/components/tools/PDFHeaderFooter';

export const metadata = {
  title: 'Add Header/Footer to PDF - Free Tool',
  description: 'Add custom headers and footers to PDF documents. Free, fast, and professional PDF header/footer tool.',
};

export default function PDFHeaderFooterPage() {
  return <PDFHeaderFooter />;
}
