import WordPasswordProtect from '@/components/tools/WordPasswordProtect';

export const metadata = {
  title: 'Word Password Protector - ToolSphere',
  description: 'Add password protection to Word documents',
};

export default function WordPasswordProtectPage() {
  return <WordPasswordProtect />;
}
