import PDFStamp from '@/components/tools/PDFStamp';

export const metadata = {
  title: 'Stamp PDF - Add Approved, Draft, Confidential Stamps',
  description: 'Add professional stamps like Approved, Draft, Confidential to PDF documents. Free PDF stamp tool.',
};

export default function PDFStampPage() {
  return <PDFStamp />;
}
