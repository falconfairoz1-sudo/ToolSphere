import IPLookup from '@/components/tools/IPLookup';

export const metadata = {
  title: 'IP Lookup - ToolSphere',
  description: 'Get information about any IP address',
};

export default function IPLookupPage() {
  return <IPLookup />;
}
