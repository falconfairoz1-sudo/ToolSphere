import PressureConverter from '@/components/tools/PressureConverter';
export const metadata = { title: 'Pressure Converter | PSI, Bar, Pascal', description: 'Convert between pressure units' };
export default function PressureConverterPage() { return <PressureConverter />; }
