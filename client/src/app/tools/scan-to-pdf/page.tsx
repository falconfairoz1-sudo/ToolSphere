import ScanToPDF from '@/components/tools/ScanToPDF';

export const metadata = {
  title: 'Scan to PDF - Convert Scanned Images to PDF',
  description: 'Convert scanned images to PDF documents. Free, fast, and easy scan to PDF converter.',
};

export default function ScanToPDFPage() {
  return <ScanToPDF />;
}
